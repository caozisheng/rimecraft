{
  "subsystems": [
    {
      "name": "GameObjects",
      "keywords": ["sprite", "text", "image", "graphics", "container", "tilesprite", "精灵", "文本", "图片", "图形", "容器"],
      "apis": [
        "this.add.sprite(x, y, key) → Phaser.GameObjects.Sprite",
        "this.add.image(x, y, key) → Phaser.GameObjects.Image",
        "this.add.text(x, y, text, style) → Phaser.GameObjects.Text",
        "this.add.graphics() → Phaser.GameObjects.Graphics",
        "this.add.tileSprite(x, y, w, h, key) → Phaser.GameObjects.TileSprite",
        "this.add.container(x, y, children?) → Phaser.GameObjects.Container",
        "this.add.particles(x, y, key, config) → Phaser.GameObjects.Particles.ParticleEmitter",
        "this.add.group(config?) → Phaser.GameObjects.Group",
        "this.add.zone(x, y, w, h) → Phaser.GameObjects.Zone",
        "this.add.rectangle(x, y, w, h, color?, alpha?) → Phaser.GameObjects.Rectangle",
        "this.add.circle(x, y, radius, color?, alpha?) → Phaser.GameObjects.Circle",
        "this.add.triangle(x, y, x1, y1, x2, y2, x3, y3, color?) → Phaser.GameObjects.Triangle",
        "this.add.line(x, y, x1, y1, x2, y2, color?, alpha?) → Phaser.GameObjects.Line",
        "this.add.star(x, y, points, innerRadius, outerRadius, color?) → Phaser.GameObjects.Star",
        "this.add.arc(x, y, radius, startAngle?, endAngle?, anticlockwise?, color?) → Phaser.GameObjects.Arc",
        "this.add.bitmapText(x, y, font, text?, size?) → Phaser.GameObjects.BitmapText",
        "this.add.nineslice(x, y, key, frame?, w, h, leftW, rightW, topH?, bottomH?) → Phaser.GameObjects.NineSlice",
        "this.add.rope(x, y, key, frame?, points?) → Phaser.GameObjects.Rope",
        "this.add.shader(key, x?, y?, w?, h?) → Phaser.GameObjects.Shader",
        "this.add.renderTexture(x, y, w?, h?) → Phaser.GameObjects.RenderTexture",
        "this.add.video(x, y, key?) → Phaser.GameObjects.Video",
        "this.add.dom(x, y, element?, style?, innerText?) → Phaser.GameObjects.DOMElement",
        "this.add.layer() → Phaser.GameObjects.Layer",
        "sprite.setOrigin(x, y)",
        "sprite.setPosition(x, y)",
        "sprite.setScale(x, y?)",
        "sprite.setAlpha(alpha)",
        "sprite.setTint(color)",
        "sprite.clearTint()",
        "sprite.setDepth(depth)",
        "sprite.setScrollFactor(x, y?)",
        "sprite.setVisible(visible)",
        "sprite.setActive(active)",
        "sprite.setInteractive(config?)",
        "sprite.setFlipX(flip) / setFlipY(flip)",
        "sprite.setAngle(degrees) / setRotation(radians)",
        "sprite.setBlendMode(mode)",
        "sprite.setMask(mask)",
        "sprite.setData(key, value) / getData(key)",
        "sprite.destroy(fromScene?)",
        "sprite.getBounds() → Phaser.Geom.Rectangle",
        "sprite.getCenter() → Phaser.Math.Vector2",
        "sprite.setName(name)",
        "text.setText(text)",
        "text.setStyle(style)",
        "text.setFontSize(size)",
        "text.setFontFamily(family)",
        "text.setColor(color)",
        "text.setStroke(color, thickness)",
        "text.setShadow(x, y, color, blur, shadowStroke?, shadowFill?)",
        "text.setWordWrapWidth(width)",
        "text.setPadding(left, top?, right?, bottom?)",
        "text.setLineSpacing(value)",
        "text.setAlign('left' | 'center' | 'right')",
        "text.setBackgroundColor(color)",
        "text.setMaxLines(max)",
        "graphics.fillStyle(color, alpha)",
        "graphics.fillRect(x, y, w, h)",
        "graphics.fillCircle(x, y, radius)",
        "graphics.fillRoundedRect(x, y, w, h, radius)",
        "graphics.fillTriangle(x1, y1, x2, y2, x3, y3)",
        "graphics.fillPoint(x, y, size)",
        "graphics.fillPath()",
        "graphics.lineStyle(width, color, alpha)",
        "graphics.strokeRect(x, y, w, h)",
        "graphics.strokeCircle(x, y, radius)",
        "graphics.strokeRoundedRect(x, y, w, h, radius)",
        "graphics.strokeTriangle(x1, y1, x2, y2, x3, y3)",
        "graphics.lineBetween(x1, y1, x2, y2)",
        "graphics.beginPath() / closePath()",
        "graphics.moveTo(x, y) / lineTo(x, y)",
        "graphics.arc(x, y, radius, startAngle, endAngle, anticlockwise?)",
        "graphics.generateTexture(key, w, h)",
        "graphics.clear()",
        "tileSprite.tilePositionX / tilePositionY",
        "tileSprite.setTilePosition(x, y)",
        "tileSprite.setTileScale(x, y?)",
        "container.add(child) / remove(child)",
        "container.addAt(child, index)",
        "container.getAt(index)",
        "container.getFirst(property?, value?)",
        "container.sort(property, handler?)",
        "container.iterate(callback)",
        "container.setExclusive(value)"
      ]
    },
    {
      "name": "Physics.Arcade",
      "keywords": ["physics", "collider", "velocity", "gravity", "bounce", "物理", "碰撞", "速度", "重力"],
      "apis": [
        "this.physics.add.sprite(x, y, key) → Phaser.Physics.Arcade.Sprite",
        "this.physics.add.image(x, y, key) → Phaser.Physics.Arcade.Image",
        "this.physics.add.group(config?) → Phaser.Physics.Arcade.Group",
        "this.physics.add.staticGroup() → Phaser.Physics.Arcade.StaticGroup",
        "this.physics.add.staticImage(x, y, key) → Phaser.Physics.Arcade.Image",
        "this.physics.add.collider(a, b, callback?, process?, scope?) → Phaser.Physics.Arcade.Collider",
        "this.physics.add.overlap(a, b, callback?, process?, scope?) → Phaser.Physics.Arcade.Collider",
        "this.physics.add.existing(gameObject, isStatic?) → gameObject",
        "this.physics.pause() / this.physics.resume()",
        "this.physics.world.setBounds(x, y, w, h, checkLeft?, checkRight?, checkUp?, checkDown?)",
        "this.physics.world.setBoundsCollision(left?, right?, up?, down?)",
        "this.physics.world.enable(object)",
        "this.physics.world.disable(object)",
        "this.physics.world.wrap(object, padding?)",
        "this.physics.world.bodies → Set<Body>",
        "this.physics.world.staticBodies → Set<StaticBody>",
        "this.physics.moveToObject(source, target, speed?, maxTime?) → angle",
        "this.physics.moveTo(source, x, y, speed?, maxTime?) → angle",
        "this.physics.accelerateToObject(source, target, speed?, xSpeedMax?, ySpeedMax?) → angle",
        "this.physics.accelerateTo(source, x, y, speed?, xSpeedMax?, ySpeedMax?) → angle",
        "this.physics.velocityFromAngle(angle, speed?, vec?) → Phaser.Math.Vector2",
        "this.physics.velocityFromRotation(rotation, speed?, vec?) → Phaser.Math.Vector2",
        "this.physics.closest(source, targets?) → Body",
        "this.physics.furthest(source, targets?) → Body",
        "body.setVelocity(x, y) / setVelocityX(x) / setVelocityY(y)",
        "body.setMaxVelocity(x, y?)",
        "body.setAcceleration(x, y) / setAccelerationX(x) / setAccelerationY(y)",
        "body.setDrag(x, y?) / setDragX(x) / setDragY(y)",
        "body.setFriction(x, y?)",
        "body.setGravityY(g) / setGravity(x, y)",
        "body.setBounce(x, y?) / setBounceX(x) / setBounceY(y)",
        "body.setCollideWorldBounds(true, bounceX?, bounceY?)",
        "body.setImmovable(true)",
        "body.setMass(mass)",
        "body.setSize(w, h, center?) / setOffset(x, y) / setCircle(radius, offsetX?, offsetY?)",
        "body.stop()",
        "body.reset(x, y)",
        "body.enable / body.enable = false",
        "body.touching.down / up / left / right",
        "body.blocked.down / up / left / right",
        "body.wasTouching.down / up / left / right",
        "body.velocity.x / velocity.y",
        "body.speed → number",
        "body.overlapX / overlapY",
        "body.embedded",
        "body.onFloor() / onCeiling() / onWall()",
        "body.setAllowGravity(value)",
        "body.checkWorldBounds()",
        "group.getChildren() → GameObject[]",
        "group.getFirst(active?, create?, x?, y?, key?) → GameObject | null",
        "group.getFirstAlive(create?, x?, y?, key?) → GameObject | null",
        "group.getFirstDead(create?, x?, y?, key?) → GameObject | null",
        "group.countActive(true) / countActive(false)",
        "group.killAndHide(obj)",
        "group.setVelocity(x, y, step?)",
        "group.setVelocityX(value, step?) / setVelocityY(value, step?)",
        "group.clear(removeFromScene?, destroyChild?)",
        "staticGroup.create(x, y, key).refreshBody()",
        "staticGroup.refresh()"
      ]
    },
    {
      "name": "Input.Keyboard",
      "keywords": ["keyboard", "key", "keys", "cursor", "WASD", "键盘", "输入", "按键"],
      "apis": [
        "this.input.keyboard!.createCursorKeys() → { left, right, up, down, space, shift }",
        "this.input.keyboard!.addKey(keyCode) → Phaser.Input.Keyboard.Key",
        "this.input.keyboard!.addKeys(keys) → Record<string, Key>",
        "this.input.keyboard!.removeKey(key)",
        "this.input.keyboard!.removeAllKeys(destroy?)",
        "Phaser.Input.Keyboard.JustDown(key) → boolean",
        "Phaser.Input.Keyboard.JustUp(key) → boolean",
        "key.isDown → boolean",
        "key.isUp → boolean",
        "key.duration → number (ms held down)",
        "key.timeDown / key.timeUp → number",
        "key.on('down', callback) / key.on('up', callback)",
        "this.input.keyboard!.on('keydown-SPACE', callback)",
        "this.input.keyboard!.on('keyup-A', callback)",
        "this.input.keyboard!.on('keydown', callback)",
        "Phaser.Input.Keyboard.KeyCodes.W / A / S / D / SPACE / ENTER / ESC / SHIFT",
        "this.input.keyboard!.enabled = false (disable keyboard)",
        "this.input.keyboard!.resetKeys()"
      ]
    },
    {
      "name": "Input.Pointer",
      "keywords": ["mouse", "pointer", "touch", "click", "drag", "鼠标", "触摸", "点击", "拖拽"],
      "apis": [
        "this.input.on('pointerdown', (pointer: Phaser.Input.Pointer) => {})",
        "this.input.on('pointerup', (pointer) => {})",
        "this.input.on('pointermove', (pointer) => {})",
        "this.input.on('pointerover', (pointer, gameObjects) => {})",
        "this.input.on('pointerout', (pointer, gameObjects) => {})",
        "this.input.on('wheel', (pointer, gameObjects, deltaX, deltaY, deltaZ) => {})",
        "sprite.setInteractive(config?) — hitArea, useHandCursor, draggable, pixelPerfect",
        "sprite.on('pointerdown', callback)",
        "sprite.on('pointerup', callback)",
        "sprite.on('pointerover', callback)",
        "sprite.on('pointerout', callback)",
        "sprite.on('dragstart', callback)",
        "sprite.on('drag', callback)",
        "sprite.on('dragend', callback)",
        "sprite.on('drop', callback)",
        "this.input.setDraggable(sprite)",
        "this.input.on('drag', (pointer, obj, dragX, dragY) => {})",
        "this.input.on('dragstart', (pointer, obj) => {})",
        "this.input.on('dragend', (pointer, obj, dropped) => {})",
        "this.input.on('drop', (pointer, obj, dropZone) => {})",
        "this.input.on('dragenter', (pointer, obj, dropZone) => {})",
        "this.input.on('dragleave', (pointer, obj, dropZone) => {})",
        "this.input.setDefaultCursor(cursor)",
        "pointer.x / pointer.y — world position",
        "pointer.worldX / pointer.worldY — world position (with camera)",
        "pointer.isDown → boolean",
        "pointer.getDuration() → number ms",
        "pointer.getDistance() → number px",
        "pointer.velocity → Phaser.Math.Vector2",
        "this.input.activePointer → Phaser.Input.Pointer",
        "this.input.x / this.input.y",
        "this.input.enabled = false (disable all input)",
        "this.input.setTopOnly(value) — only top object gets events"
      ]
    },
    {
      "name": "Scene",
      "keywords": ["scene", "start", "launch", "restart", "场景", "切换", "启动", "生命周期"],
      "apis": [
        "this.scene.start(key, data?)",
        "this.scene.restart(data?)",
        "this.scene.launch(key, data?)",
        "this.scene.stop(key?)",
        "this.scene.pause(key?)",
        "this.scene.resume(key?, data?)",
        "this.scene.sleep(key?)",
        "this.scene.wake(key?, data?)",
        "this.scene.switch(key)",
        "this.scene.run(key, data?)",
        "this.scene.get(key) → Phaser.Scene",
        "this.scene.isActive(key) → boolean",
        "this.scene.isPaused(key) → boolean",
        "this.scene.isSleeping(key) → boolean",
        "this.scene.isVisible(key) → boolean",
        "this.scene.bringToTop(key?)",
        "this.scene.sendToBack(key?)",
        "this.scene.moveUp(key?) / moveDown(key?)",
        "this.scene.moveAbove(keyA, keyB) / moveBelow(keyA, keyB)",
        "this.scene.setActive(value, key?)",
        "this.scene.setVisible(value, key?)",
        "create(data) — receives data from scene.start()",
        "init(data) — called before preload, receives data",
        "preload() — load assets",
        "create() — setup game objects",
        "update(time, delta) — game loop, time=total ms, delta=frame ms",
        "this.events.on('create', callback)",
        "this.events.on('pause', callback)",
        "this.events.on('resume', callback)",
        "this.events.on('sleep', callback)",
        "this.events.on('wake', callback)",
        "this.events.on('shutdown', callback)",
        "this.events.on('destroy', callback)",
        "this.events.on('update', callback)",
        "this.events.emit(event, ...args)",
        "this.sys.game → Phaser.Game"
      ]
    },
    {
      "name": "Camera",
      "keywords": ["camera", "follow", "shake", "fade", "zoom", "相机", "跟随", "震动", "缩放"],
      "apis": [
        "this.cameras.main → Phaser.Cameras.Scene2D.Camera",
        "this.cameras.add(x?, y?, w?, h?) → Camera",
        "this.cameras.remove(camera)",
        "camera.startFollow(target, roundPixels?, lerpX?, lerpY?, offsetX?, offsetY?)",
        "camera.stopFollow()",
        "camera.setBounds(x, y, w, h, centerOn?)",
        "camera.setDeadzone(w, h)",
        "camera.setFollowOffset(x, y)",
        "camera.setZoom(zoom)",
        "camera.zoomTo(zoom, duration, ease?, force?, callback?)",
        "camera.setScroll(x, y)",
        "camera.scrollX / scrollY",
        "camera.centerOn(x, y)",
        "camera.centerToBounds()",
        "camera.pan(x, y, duration, ease?, force?, callback?)",
        "camera.shake(duration, intensity?, force?, callback?)",
        "camera.flash(duration, r?, g?, b?, force?, callback?)",
        "camera.fadeOut(duration, r?, g?, b?, callback?)",
        "camera.fadeIn(duration, r?, g?, b?, callback?)",
        "camera.fadeFrom(duration, r?, g?, b?, force?, callback?)",
        "camera.fadeTo(duration, r?, g?, b?, force?, callback?)",
        "camera.once('camerafadeoutcomplete', callback)",
        "camera.once('camerafadeincomplete', callback)",
        "camera.once('camerashakecomplete', callback)",
        "camera.once('camerapancomplete', callback)",
        "camera.setBackgroundColor(color)",
        "camera.setViewport(x, y, w, h)",
        "camera.setSize(w, h)",
        "camera.worldView → Phaser.Geom.Rectangle",
        "camera.getWorldPoint(x, y) → Phaser.Math.Vector2",
        "camera.ignore(gameObject | gameObjects[])",
        "camera.setAlpha(alpha)",
        "camera.setRoundPixels(value)",
        "camera.setRotation(radians)",
        "camera.setName(name)"
      ]
    },
    {
      "name": "Tweens",
      "keywords": ["tween", "animate", "ease", "补间", "动画", "缓动"],
      "apis": [
        "this.tweens.add({ targets, props, duration, ease, repeat?, yoyo?, delay?, onComplete? }) → Tween",
        "this.tweens.chain({ targets, tweens: [...], loop?, repeat? }) → TweenChain",
        "this.tweens.addCounter({ from, to, duration, ease, onUpdate }) → Tween",
        "this.tweens.killAll()",
        "this.tweens.killTweensOf(target)",
        "this.tweens.getTweensOf(target) → Tween[]",
        "this.tweens.isTweening(target) → boolean",
        "this.tweens.pauseAll() / resumeAll()",
        "tween.stop() / tween.pause() / tween.resume()",
        "tween.remove()",
        "tween.restart()",
        "tween.complete()",
        "tween.isPlaying() → boolean",
        "tween.isPaused() → boolean",
        "tween.seek(toPosition)",
        "tween.on('complete', callback)",
        "tween.on('update', callback)",
        "tween.on('loop', callback)",
        "tween.on('repeat', callback)",
        "tween.on('yoyo', callback)",
        "Ease functions: 'Linear', 'Power1/2/3/4', 'Quad.In/Out/InOut', 'Cubic.In/Out/InOut', 'Quart.In/Out/InOut', 'Quint.In/Out/InOut', 'Sine.In/Out/InOut', 'Expo.In/Out/InOut', 'Circ.In/Out/InOut', 'Back.In/Out/InOut', 'Bounce.In/Out/InOut', 'Elastic.In/Out/InOut', 'Stepped(steps)'"
      ]
    },
    {
      "name": "Time",
      "keywords": ["timer", "delay", "clock", "计时", "延迟", "时间"],
      "apis": [
        "this.time.addEvent({ delay, callback, callbackScope?, args?, loop?, repeat?, startAt?, paused? }) → TimerEvent",
        "this.time.delayedCall(delay, callback, args?, scope?) → TimerEvent",
        "this.time.removeEvent(event) / this.time.removeAllEvents()",
        "this.time.paused = true / false",
        "this.time.now → number",
        "this.time.timeScale = value",
        "timerEvent.remove()",
        "timerEvent.getElapsed() → number",
        "timerEvent.getOverallProgress() → number",
        "timerEvent.getRepeatCount() → number",
        "timerEvent.getProgress() → number",
        "timerEvent.paused = true / false",
        "timerEvent.reset(config)",
        "update(time: number, delta: number) — time = total ms, delta = ms since last frame"
      ]
    },
    {
      "name": "Loader",
      "keywords": ["load", "preload", "asset", "加载", "资源", "预加载"],
      "apis": [
        "this.load.image(key, url)",
        "this.load.spritesheet(key, url, { frameWidth, frameHeight, startFrame?, endFrame?, margin?, spacing? })",
        "this.load.audio(key, url | url[])",
        "this.load.atlas(key, textureUrl, atlasUrl)",
        "this.load.multiatlas(key, atlasUrl, path?)",
        "this.load.tilemapTiledJSON(key, url)",
        "this.load.tilemapCSV(key, url)",
        "this.load.bitmapFont(key, textureUrl, fontDataUrl)",
        "this.load.json(key, url)",
        "this.load.text(key, url)",
        "this.load.xml(key, url)",
        "this.load.html(key, url)",
        "this.load.css(key, url)",
        "this.load.script(key, url)",
        "this.load.svg(key, url, svgConfig?)",
        "this.load.video(key, url, loadEvent?, asBlob?, noAudio?)",
        "this.load.plugin(key, url, start?)",
        "this.load.setBaseURL(url)",
        "this.load.setPath(path)",
        "this.load.setCORS(cors)",
        "this.load.on('progress', (value: number) => {}) — 0..1",
        "this.load.on('fileprogress', (file, percentComplete) => {})",
        "this.load.on('complete', () => {})",
        "this.load.on('loaderror', (file) => {})",
        "this.load.start() — start loading (auto in preload)",
        "this.load.totalToLoad / this.load.totalComplete",
        "this.textures.exists(key) → boolean",
        "this.textures.get(key) → Texture",
        "this.textures.remove(key)",
        "this.cache.json.get(key) → object",
        "this.cache.text.get(key) → string"
      ]
    },
    {
      "name": "Animation",
      "keywords": ["animation", "anim", "frame", "动画", "帧", "精灵动画"],
      "apis": [
        "this.anims.create({ key, frames, frameRate, repeat, yoyo?, delay?, repeatDelay?, hideOnComplete? }) → Animation",
        "this.anims.generateFrameNumbers(key, { start, end, first?, frames? }) → AnimationFrame[]",
        "this.anims.generateFrameNames(key, { prefix?, start?, end?, suffix?, zeroPad?, frames? }) → AnimationFrame[]",
        "this.anims.exists(key) → boolean",
        "this.anims.get(key) → Animation",
        "this.anims.remove(key) → Animation",
        "this.anims.pauseAll() / resumeAll()",
        "this.anims.staggerPlay(key, targets, stagger)",
        "sprite.play(animKey, ignoreIfPlaying?)",
        "sprite.playReverse(animKey, ignoreIfPlaying?)",
        "sprite.playAfterDelay(animKey, delay)",
        "sprite.playAfterRepeat(animKey, repeatCount?)",
        "sprite.anims.play(animKey, ignoreIfPlaying?)",
        "sprite.anims.stop()",
        "sprite.anims.pause() / resume()",
        "sprite.anims.restart()",
        "sprite.anims.setRepeat(value)",
        "sprite.anims.setTimeScale(value)",
        "sprite.anims.getName() → string",
        "sprite.anims.getFrameName() → string",
        "sprite.anims.isPlaying → boolean",
        "sprite.anims.currentAnim → Animation",
        "sprite.anims.currentFrame → AnimationFrame",
        "sprite.anims.msPerFrame → number",
        "sprite.on('animationcomplete', callback)",
        "sprite.on('animationcomplete-KEY', callback)",
        "sprite.on('animationstart', callback)",
        "sprite.on('animationupdate', callback)",
        "sprite.on('animationrepeat', callback)"
      ]
    },
    {
      "name": "Math",
      "keywords": ["random", "math", "distance", "angle", "vector", "随机", "数学", "距离", "角度"],
      "apis": [
        "Phaser.Math.Between(min, max) → integer",
        "Phaser.Math.FloatBetween(min, max) → float",
        "Phaser.Math.Distance.Between(x1, y1, x2, y2) → number",
        "Phaser.Math.Distance.BetweenPoints(a, b) → number",
        "Phaser.Math.Distance.BetweenPointsSquared(a, b) → number",
        "Phaser.Math.Angle.Between(x1, y1, x2, y2) → radians",
        "Phaser.Math.Angle.BetweenPoints(a, b) → radians",
        "Phaser.Math.Angle.Wrap(angle) → radians",
        "Phaser.Math.Angle.ShortestBetween(angle1, angle2) → radians",
        "Phaser.Math.Clamp(value, min, max) → number",
        "Phaser.Math.Wrap(value, min, max) → number",
        "Phaser.Math.Snap.Floor(value, gap, start?) → number",
        "Phaser.Math.Snap.Ceil(value, gap, start?) → number",
        "Phaser.Math.Snap.To(value, gap, start?) → number",
        "Phaser.Math.Linear(a, b, t) → number (lerp)",
        "Phaser.Math.SmoothStep(x, min, max) → number",
        "Phaser.Math.SmootherStep(x, min, max) → number",
        "Phaser.Math.Percent(value, min, max, upperMax?) → 0..1",
        "Phaser.Math.DegToRad(degrees) / Phaser.Math.RadToDeg(radians)",
        "Phaser.Math.Easing.* — all easing functions",
        "new Phaser.Math.Vector2(x, y)",
        "vec.add(src) / subtract(src) / multiply(src) / divide(src)",
        "vec.scale(value)",
        "vec.normalize() → this",
        "vec.length() → number",
        "vec.distance(src) → number",
        "vec.angle() → radians",
        "vec.setAngle(angle) → this",
        "vec.rotate(delta) → this",
        "vec.lerp(src, t) → this",
        "vec.dot(src) → number",
        "vec.cross(src) → number",
        "vec.clone() → Vector2",
        "Phaser.Utils.Array.GetRandom(array) → item",
        "Phaser.Utils.Array.Shuffle(array) → array",
        "Phaser.Utils.Array.Remove(array, item) → item",
        "Phaser.Utils.Array.MoveUp(array, item) → array",
        "Phaser.Utils.Array.RotateLeft(array, total?) → item",
        "Phaser.Utils.Array.RotateRight(array, total?) → item"
      ]
    },
    {
      "name": "Sound",
      "keywords": ["sound", "audio", "music", "sfx", "声音", "音频", "音乐", "音效"],
      "apis": [
        "this.sound.add(key, config?) → Sound",
        "this.sound.play(key, config?)",
        "this.sound.get(key) → Sound",
        "this.sound.getAll(key) → Sound[]",
        "this.sound.removeByKey(key)",
        "this.sound.removeAll()",
        "this.sound.stopAll()",
        "this.sound.pauseAll() / resumeAll()",
        "this.sound.unlock()",
        "this.sound.locked → boolean",
        "this.sound.mute = boolean",
        "this.sound.volume = number",
        "this.sound.rate = number",
        "this.sound.detune = number",
        "sound.play(config?)",
        "sound.stop()",
        "sound.pause() / resume()",
        "sound.setVolume(vol) / setLoop(loop)",
        "sound.setRate(rate) / setDetune(detune)",
        "sound.setSeek(value)",
        "sound.setMute(value)",
        "sound.isPlaying → boolean",
        "sound.isPaused → boolean",
        "sound.totalDuration → number",
        "sound.seek → number",
        "sound.on('play', callback)",
        "sound.on('complete', callback)",
        "sound.on('looped', callback)",
        "sound.on('pause', callback)",
        "sound.on('resume', callback)",
        "sound.on('stop', callback)",
        "sound.addMarker({ name, start, duration, config? })",
        "sound.play(markerName)"
      ]
    },
    {
      "name": "Scale",
      "keywords": ["scale", "resize", "fullscreen", "responsive", "缩放", "全屏", "适配", "分辨率"],
      "apis": [
        "this.scale.width → number",
        "this.scale.height → number",
        "this.scale.gameSize → Phaser.Structs.Size",
        "this.scale.displaySize → Phaser.Structs.Size",
        "this.scale.parentSize → Phaser.Structs.Size",
        "this.scale.resize(w, h)",
        "this.scale.setGameSize(w, h)",
        "this.scale.setZoom(value)",
        "this.scale.startFullscreen()",
        "this.scale.stopFullscreen()",
        "this.scale.toggleFullscreen()",
        "this.scale.isFullscreen → boolean",
        "this.scale.lockOrientation(orientation)",
        "this.scale.setParentSize(w, h)",
        "this.scale.setMaxZoom() → number",
        "this.scale.on('resize', (gameSize, baseSize, displaySize, previousWidth, previousHeight) => {})",
        "this.scale.on('orientationchange', (orientation) => {})",
        "this.scale.on('enterfullscreen', callback)",
        "this.scale.on('leavefullscreen', callback)",
        "Phaser.Scale.FIT — scale to fit, maintain aspect ratio",
        "Phaser.Scale.RESIZE — canvas matches container",
        "Phaser.Scale.ENVELOP — fill container, may crop",
        "Phaser.Scale.NONE — no scaling",
        "Phaser.Scale.CENTER_BOTH / CENTER_HORIZONTALLY / CENTER_VERTICALLY / NO_CENTER"
      ]
    },
    {
      "name": "Particles",
      "keywords": ["particle", "emitter", "explosion", "effect", "粒子", "特效", "爆炸", "发射器"],
      "apis": [
        "this.add.particles(x, y, key, config?) → Phaser.GameObjects.Particles.ParticleEmitter",
        "emitter.explode(count?, x?, y?) — one-shot burst",
        "emitter.emitParticle(count?, x?, y?)",
        "emitter.emitParticleAt(x, y, count?)",
        "emitter.start() / stop()",
        "emitter.pause() / resume()",
        "emitter.setConfig(config)",
        "emitter.setPosition(x, y)",
        "emitter.setSpeed(value | { min, max })",
        "emitter.setAngle(value | { min, max })",
        "emitter.setScale(value | { start, end })",
        "emitter.setAlpha(value | { start, end })",
        "emitter.setLifespan(value | { min, max })",
        "emitter.setFrequency(frequency, quantity?)",
        "emitter.setQuantity(quantity)",
        "emitter.setTint(value | [values])",
        "emitter.setGravity(x, y) / setGravityX(x) / setGravityY(y)",
        "emitter.setBounds(x, y, w, h) / setBounds(rect)",
        "emitter.setFrame(frames | frame)",
        "emitter.setEmitZone({ type, source, quantity?, stepRate?, yoyo? })",
        "emitter.setDeathZone({ type, source })",
        "emitter.setParticleRotation(value | { min, max })",
        "emitter.follow(target, offsetX?, offsetY?, trackVisible?)",
        "emitter.stopFollow()",
        "emitter.setBlendMode(mode)",
        "emitter.setVisible(value)",
        "emitter.setDepth(value)",
        "emitter.killAll()",
        "emitter.destroy()",
        "config: { speed, angle, scale, alpha, lifespan, frequency, quantity, tint, blendMode, emitting, maxParticles, gravityX, gravityY, bounce, accelerationX, accelerationY, rotate, follow }"
      ]
    },
    {
      "name": "Geometry",
      "keywords": ["geometry", "rectangle", "circle", "line", "point", "几何", "矩形", "圆", "线段", "点"],
      "apis": [
        "new Phaser.Geom.Rectangle(x, y, w, h)",
        "rect.contains(x, y) → boolean",
        "rect.setTo(x, y, w, h)",
        "rect.centerX / centerY",
        "rect.width / height / left / right / top / bottom",
        "Phaser.Geom.Rectangle.Overlaps(rectA, rectB) → boolean",
        "Phaser.Geom.Rectangle.Contains(rect, x, y) → boolean",
        "Phaser.Geom.Rectangle.ContainsPoint(rect, point) → boolean",
        "Phaser.Geom.Rectangle.Intersection(rectA, rectB, out?) → Rectangle",
        "Phaser.Geom.Rectangle.Random(rect, out?) → Point",
        "new Phaser.Geom.Circle(x, y, radius)",
        "circle.contains(x, y) → boolean",
        "Phaser.Geom.Circle.Contains(circle, x, y) → boolean",
        "Phaser.Geom.Circle.Random(circle, out?) → Point",
        "new Phaser.Geom.Line(x1, y1, x2, y2)",
        "Phaser.Geom.Line.Length(line) → number",
        "Phaser.Geom.Line.Random(line, out?) → Point",
        "Phaser.Geom.Line.GetMidPoint(line, out?) → Point",
        "Phaser.Geom.Line.NormalAngle(line) → number",
        "new Phaser.Geom.Point(x, y)",
        "new Phaser.Geom.Triangle(x1, y1, x2, y2, x3, y3)",
        "Phaser.Geom.Triangle.Contains(triangle, x, y) → boolean",
        "new Phaser.Geom.Polygon(points)",
        "Phaser.Geom.Polygon.Contains(polygon, x, y) → boolean",
        "Phaser.Geom.Intersects.RectangleToRectangle(rectA, rectB) → boolean",
        "Phaser.Geom.Intersects.CircleToRectangle(circle, rect) → boolean",
        "Phaser.Geom.Intersects.LineToRectangle(line, rect) → boolean",
        "Phaser.Geom.Intersects.LineToLine(lineA, lineB, out?) → boolean",
        "Phaser.Geom.Intersects.PointToLine(point, line, lineThickness?) → boolean"
      ]
    },
    {
      "name": "Tilemap",
      "keywords": ["tilemap", "tile", "map", "tiled", "layer", "瓦片", "地图", "关卡", "图层"],
      "apis": [
        "this.make.tilemap({ key }) → Phaser.Tilemaps.Tilemap",
        "this.make.tilemap({ data, tileWidth, tileHeight }) → Tilemap",
        "map.addTilesetImage(tilesetName, imageKey?, tileWidth?, tileHeight?, tileMargin?, tileSpacing?) → Tileset | null",
        "map.createLayer(layerID, tileset, x?, y?) → TilemapLayer | null",
        "map.createBlankLayer(name, tileset, x?, y?, w?, h?, tileW?, tileH?) → TilemapLayer",
        "layer.setCollision(indexes, collides?, recalculateFaces?)",
        "layer.setCollisionBetween(start, end, collides?)",
        "layer.setCollisionByProperty({ propertyName: value })",
        "layer.setCollisionByExclusion(indexes, collides?)",
        "layer.setCollisionFromCollisionGroup(collides?)",
        "layer.setTileIndexCallback(indexes, callback, scope)",
        "layer.setTileLocationCallback(tileX, tileY, w, h, callback, scope)",
        "layer.getTileAt(tileX, tileY, nonNull?) → Tile | null",
        "layer.getTileAtWorldXY(worldX, worldY, nonNull?, camera?) → Tile | null",
        "layer.putTileAt(tile, tileX, tileY, recalculateFaces?)",
        "layer.putTileAtWorldXY(tile, worldX, worldY, recalculateFaces?, camera?)",
        "layer.removeTileAt(tileX, tileY, replaceWithNull?, recalculateFaces?)",
        "layer.fill(index, tileX?, tileY?, w?, h?, recalculateFaces?)",
        "layer.weightedRandomize(weightedIndexes, tileX?, tileY?, w?, h?)",
        "layer.filterTiles(callback, scope?, options?) → Tile[]",
        "layer.forEachTile(callback, scope?, options?)",
        "layer.findByIndex(index, skip?, reverse?) → Tile | null",
        "layer.worldToTileX(worldX, snapToFloor?, camera?) → number",
        "layer.worldToTileY(worldY, snapToFloor?, camera?) → number",
        "layer.tileToWorldX(tileX, camera?) → number",
        "layer.tileToWorldY(tileY, camera?) → number",
        "layer.setAlpha(alpha) / setVisible(visible) / setDepth(depth)",
        "layer.setTileSize(tileWidth, tileHeight)",
        "map.getObjectLayer(name) → ObjectLayer",
        "objectLayer.objects → TiledObject[] — { name, type, x, y, width, height, properties }",
        "map.widthInPixels / heightInPixels",
        "map.width / height (in tiles)"
      ]
    },
    {
      "name": "GameConfig",
      "keywords": ["config", "game", "setup", "init", "canvas", "配置", "初始化", "画布"],
      "apis": [
        "new Phaser.Game(config: Phaser.Types.Core.GameConfig)",
        "config.type: Phaser.AUTO | Phaser.WEBGL | Phaser.CANVAS",
        "config.width: number — game width in pixels",
        "config.height: number — game height in pixels",
        "config.parent: string | HTMLElement — DOM element to mount canvas",
        "config.backgroundColor: string — hex color e.g. '#2d2d2d'",
        "config.transparent: boolean",
        "config.pixelArt: boolean — disable anti-aliasing for pixel art",
        "config.antialias: boolean — enable/disable antialiasing",
        "config.roundPixels: boolean — round positions to integers",
        "config.scene: Scene | Scene[] | SceneConfig | SceneConfig[] — scene list",
        "config.physics: { default: 'arcade' | 'matter', arcade?: ArcadeConfig, matter?: MatterConfig }",
        "config.physics.arcade: { gravity: { x, y }, debug: boolean, debugShowBody?, debugShowStaticBody?, debugBodyColor? }",
        "config.scale: { mode, autoCenter, width, height, min?, max?, parent?, zoom?, expandParent? }",
        "config.input: { keyboard?: boolean | KeyboardConfig, mouse?: boolean | MouseConfig, touch?: boolean | TouchConfig, gamepad?: boolean }",
        "config.audio: { disableWebAudio?: boolean, noAudio?: boolean }",
        "config.render: { pixelArt?, antialias?, antialiasGL?, desynchronized?, powerPreference?, batchSize? }",
        "config.fps: { target?: number, forceSetTimeOut?: boolean, min?: number }",
        "config.callbacks: { preBoot?: Function, postBoot?: Function }",
        "config.dom: { createContainer?: boolean }",
        "config.banner: { hidePhaser?: boolean, text?, background? }",
        "this.sys.game.config → GameConfig (read-only)",
        "this.sys.game.destroy(removeCanvas?, noReturn?)"
      ]
    },
    {
      "name": "DataManager",
      "keywords": ["data", "registry", "store", "state", "数据", "存储", "状态", "注册表"],
      "apis": [
        "sprite.setData(key, value) / sprite.setData({ key: value })",
        "sprite.getData(key) → any",
        "sprite.data.set(key, value)",
        "sprite.data.get(key) → any",
        "sprite.data.has(key) → boolean",
        "sprite.data.remove(key)",
        "sprite.data.getAll() → object",
        "sprite.data.values → object",
        "sprite.data.count → number",
        "sprite.data.each(callback, scope?)",
        "this.registry.set(key, value)",
        "this.registry.get(key) → any",
        "this.registry.has(key) → boolean",
        "this.registry.remove(key)",
        "this.registry.getAll() → object",
        "this.registry.events.on('changedata', (parent, key, data, previousData) => {})",
        "this.registry.events.on('changedata-KEY', (parent, data, previousData) => {})",
        "this.registry.events.on('setdata', (parent, key, data) => {})",
        "this.registry.events.on('removedata', (parent, key, data) => {})"
      ]
    },
    {
      "name": "Display",
      "keywords": ["color", "mask", "blend", "display", "颜色", "遮罩", "混合", "显示"],
      "apis": [
        "Phaser.Display.Color.HexStringToColor(hex) → Color",
        "Phaser.Display.Color.IntegerToColor(input) → Color",
        "Phaser.Display.Color.ValueToColor(input) → Color",
        "Phaser.Display.Color.GetColor(r, g, b) → integer",
        "Phaser.Display.Color.GetColor32(r, g, b, a) → integer",
        "Phaser.Display.Color.RandomRGB(min?, max?) → Color",
        "Phaser.Display.Color.Interpolate.ColorWithColor(color1, color2, length, index) → ColorObject",
        "new Phaser.Display.Color(r?, g?, b?, a?)",
        "color.setTo(r, g, b, a?) → this",
        "color.setFromRGB(color) → this",
        "color.transparent() → this",
        "color.color → integer",
        "color.color32 → integer",
        "color.rgba → string",
        "color.red / green / blue / alpha → number",
        "sprite.setBlendMode(mode)",
        "Phaser.BlendModes: NORMAL, ADD, MULTIPLY, SCREEN, ERASE, DIFFERENCE",
        "sprite.setMask(mask) / sprite.clearMask(destroy?)",
        "this.add.graphics().createGeometryMask() → GeometryMask",
        "sprite.createBitmapMask(renderTexture?) → BitmapMask",
        "mask.setInvertAlpha(value)"
      ]
    },
    {
      "name": "Events",
      "keywords": ["event", "emit", "listener", "signal", "事件", "监听", "信号", "通知"],
      "apis": [
        "this.events.on(event, callback, scope?)",
        "this.events.once(event, callback, scope?)",
        "this.events.off(event, callback?, scope?)",
        "this.events.emit(event, ...args)",
        "this.events.removeAllListeners(event?)",
        "this.events.listenerCount(event) → number",
        "this.game.events.on('blur', callback) — game loses focus",
        "this.game.events.on('focus', callback) — game gains focus",
        "this.game.events.on('pause', callback) — game paused",
        "this.game.events.on('resume', callback) — game resumed",
        "this.game.events.on('visible', callback) — tab visible",
        "this.game.events.on('hidden', callback) — tab hidden",
        "this.game.events.on('step', callback) — each game step",
        "this.game.events.on('prestep', callback)",
        "this.game.events.on('poststep', callback)",
        "this.game.events.on('prerender', callback)",
        "this.game.events.on('postrender', callback)",
        "Scene events: 'create', 'update', 'pause', 'resume', 'sleep', 'wake', 'shutdown', 'destroy'"
      ]
    }
  ]
}
